import { component$ } from '@qwik.dev/core';
import { SlashIcon } from 'lucide-qwik';

export default component$(() => {
	return (
		<SlashIcon class="shrink-0 h-5 w-5 text-gray-300" />
	);
});
